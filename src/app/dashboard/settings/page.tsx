import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function SettingsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Settings</CardTitle>
        <CardDescription>
          Manage your account and application settings. This section is under construction.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Tabs for Profile, Security, Notifications, and other settings will be available here.</p>
      </CardContent>
    </Card>
  );
}
