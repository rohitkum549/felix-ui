import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function TransactionsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Transactions</CardTitle>
        <CardDescription>
          A detailed history of all your transactions. This section is under construction.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>A filterable and searchable table of all transactions will be displayed here.</p>
      </CardContent>
    </Card>
  );
}
